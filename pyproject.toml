[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # regellengte is een aanbeveling, geen harde eis hier

[tool.pytest.ini_options]
testpaths = ["tests"]
